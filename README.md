# Employee Management System - Full Stack CRUD Application

A full-stack web application developed as a practical assignment for **Lesson 08: Backend with MongoDB** This project demonstrates a complete CRUD (Create, Read, Update, Delete) workflow using Spring Boot for the backend and a Vanilla JavaScript frontend, connected to a MongoDB Atlas database.

## 📘 Project Overview
This application serves as a demo for connecting a Spring Boot RESTful API with a NoSQL database (MongoDB) and consuming that API using a simple frontend interface.

## 🚀 Features

**Add Employee:** Form to create new employee records (First Name, Last Name, Email).
**View Employees:** Dynamic table display of all employees stored in the database.
**Update Employee:** Functionality to edit existing employee details.
**Delete Employee:** Remove employee records from the database.
**RESTful API:** Backend follows the Controller-Service-Repository pattern.

## 🛠️ Tech Stack

**Backend:**
* **Java** (JDK 17 or 21)
* **Spring Boot** (v3.x)
* **Dependencies:** Spring Web, Spring Data MongoDB
* **Build Tool:** Maven 

**Database:**
**MongoDB Atlas** (Cloud NoSQL Database)

**Frontend:**
**HTML5 / CSS3** (Custom styling for forms and tables) 
**JavaScript** (Vanilla JS using the Fetch API)

